name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Install Qt
      uses: jurplel/install-qt-action@v1
    - name: qmake
      run: qmake
    - name: make
      run: make
    - name: make check
      run: make check
